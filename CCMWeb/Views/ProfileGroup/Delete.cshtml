
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<CCM.Web.Properties.Resources> Localizer
@model CCM.Core.Entities.ProfileGroup

@{
    ViewData["Title"] = @Localizer["Delete_ProfileGroup"];
}

<h2>@Localizer["Delete_ProfileGroup"]</h2>
<hr />

<p>@Localizer["Delete_ProfileGroup_Are_You_Sure"]</p>
<p>@Model.Name</p>

@using (Html.BeginForm("Delete", "ProfileGroup", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    @Html.HiddenFor(m => m.Id)

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            @Html.ActionLink(@Localizer["Cancel"].Value, "Index", "ProfileGroup", null, new { @class = "btn btn-default"})
            <button type="submit" class="btn btn-danger">@Localizer["Delete"]</button>
        </div>
    </div>
}